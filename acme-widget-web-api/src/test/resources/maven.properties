# since this file in in maven src/main/resources folder,
# maven will process it replacing maven properties such as ${project.version},
# so creating properties here that are set to maven properties will allow
# access from the run-time code if required...
#
contextRoot=/${project.artifactId}

